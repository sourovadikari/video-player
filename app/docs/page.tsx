import Link from "next/link";

const topics = ["Getting started", "Basic video", "Basic audio", "Captions and chapters", "HLS and quality", "Media Session", "Headless mode", "Events and ref API", "Accessibility", "Browser support"];
export default function DocsPage() { return <main className="app-shell"><nav className="site-nav inner-nav"><Link className="brand" href="/">signal<span>play</span></Link><div className="nav-links"><Link href="/playground">Playground</Link><Link href="/examples">Examples</Link></div><Link className="nav-cta" href="/">Back home <span>↗</span></Link></nav><div className="docs-layout"><aside className="docs-sidebar"><span className="section-kicker">Documentation</span>{topics.map((topic, index) => <a href={`#topic-${index}`} key={topic}>{String(index + 1).padStart(2, "0")} {topic}</a>)}</aside><article className="docs-content"><span className="section-kicker">A small API with a wide surface</span><h1>Build around the browser.</h1><p className="docs-intro">signalplay is a reusable media component, not an application shell. Pass in a source, configure the controls you need, and keep routing, metadata, queues, and persistence in the host app.</p><section id="topic-0"><h2>Getting started</h2><pre><code>{`import { MediaPlayer } from "@/components/media-player";\n\n<MediaPlayer\n  type="video"\n  src="/media/intro.mp4"\n  controls\n/>`}</code></pre></section><section id="topic-1"><h2>Video and audio</h2><p>Use <code>type=&quot;video&quot;</code> for a visual player or <code>type=&quot;audio&quot;</code> for native background-compatible audio. Both share the same playback state and imperative ref API.</p></section><section id="topic-2"><h2>Tracks and streaming</h2><p>Captions and chapters are WebVTT tracks. Native HLS is used when available. Multiple real sources can be passed through <code>quality</code>; the player preserves time and playing state during a switch.</p></section><section id="topic-3"><h2>Headless mode</h2><p>Set <code>controls={`{false}`}</code> to render no default UI. The typed ref exposes play, pause, seek, volume, rate, fullscreen, and Picture-in-Picture actions, while events expose state to your own controls.</p></section><section id="topic-4"><h2>Responsibility boundary</h2><div className="boundary-grid"><div><strong>Host application</strong><span>Routes, metadata, queues, persistence, analytics, auth</span></div><div><strong>Player</strong><span>Playback, controls, state, tracks, browser APIs, accessibility</span></div></div></section></article></div></main>; }