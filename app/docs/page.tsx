import Link from "next/link";

const topics = ["Getting started", "Captions and chapters", "Quality sources", "Playlist navigation", "Media Session", "Headless mode", "Events and ref API", "Accessibility", "Browser support"];
export default function DocsPage() { return <main className="app-shell"><nav className="site-nav inner-nav"><Link className="brand" href="/">signal<span>play</span></Link><div className="nav-links"><Link href="/playground">Playground</Link><Link href="/examples">Examples</Link></div><Link className="nav-cta" href="/">Back home <span>↗</span></Link></nav><div className="docs-layout"><aside className="docs-sidebar"><span className="section-kicker">Documentation</span>{topics.map((topic, index) => <a href={`#topic-${index}`} key={topic}>{String(index + 1).padStart(2, "0")} {topic}</a>)}</aside><article className="docs-content"><span className="section-kicker">A small API with a wide surface</span><h1>Build around the browser.</h1><p className="docs-intro">signalplay is a reusable video component, not an application shell. Pass in a source, configure the controls you need, and keep routing and metadata in the host app.</p><section id="topic-0"><h2>Getting started</h2><pre><code>{`import { MediaPlayer } from "@/components/media-player";\n\n<MediaPlayer\n  src="/media/demo.mp4"\n  controls\n/>`}</code></pre></section><section id="topic-1"><h2>Tracks and captions</h2><p>Captions and chapters are WebVTT tracks. Pass them through <code>captions</code> and <code>chapters</code>; the CC control only appears when captions exist.</p></section><section id="topic-2"><h2>Quality sources</h2><p>Pass multiple real source URLs through <code>quality</code>. The player only shows Quality when at least two supplied sources exist, preserves the current time, and resumes playback after switching.</p></section><section id="topic-3"><h2>Playlist navigation</h2><p>Pass <code>hasPrevious</code>/<code>hasNext</code> and <code>onPrevious</code>/<code>onNext</code> to enable the Previous/Next Video controls. They only render when a handler is actually supplied — never as fake, non-functional buttons — and each is disabled at the corresponding end of your list. The dedicated ±<code>seekStep</code> double-tap/double-click gesture (toggle it off with <code>doubleTapSeek={`{false}`}</code>, or change the amount with <code>seekStep</code>) is separate from Previous/Next and keeps working alongside it. Set <code>landscapeOnFullscreen={`{false}`}</code> to skip the mobile orientation-lock attempt on fullscreen.</p></section><section id="topic-4"><h2>Media Session</h2><p>Provide <code>mediaSession</code> metadata to connect play, pause, and seek actions to supported device controls.</p></section><section id="topic-5"><h2>Headless mode</h2><p>Set <code>controls={`{false}`}</code> to render no default UI, or pass a <code>controls</code> object to toggle individual buttons (including <code>speed</code> and <code>quality</code> rows inside the Settings menu). The typed ref exposes play, pause, seek, volume, rate, fullscreen, and Picture-in-Picture actions.</p></section><section id="topic-6"><h2>Events and ref API</h2><p>Playback state is synchronized from the native video element events, including metadata, duration, time, buffering, volume, and rate changes.</p></section><section id="topic-7"><h2>Accessibility</h2><p>Controls are semantic buttons and sliders with labels, focus states, keyboard shortcuts, and menu roles.</p></section><section id="topic-8"><h2>Browser support</h2><p>The player uses standard HTMLVideoElement APIs and gracefully reports unsupported fullscreen or Picture-in-Picture features.</p></section></article></div></main>; }
